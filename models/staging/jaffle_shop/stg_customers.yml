version: 2

models:
  - name: stg_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
    description: Staging of raw_customers source data. We rename columns to enable merging with other staging tables.
    config:
      tags: [jaffle_shop]
    meta:
      owner: '@ran'
      model_maturity: in dev
